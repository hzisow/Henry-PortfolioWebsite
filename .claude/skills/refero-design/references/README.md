# Saved DESIGN.md references

Drop `DESIGN.md` files copied from https://styles.refero.design here, named per brand,
e.g. `linear.md`, `stripe.md`. The refero-design skill reads these when the live Refero
MCP isn't connected (the site blocks automated fetching, so they must be saved manually).
